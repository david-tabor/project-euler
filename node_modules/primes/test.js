

var modules_path = 'C:/Users/David Tabor/github/project-euler/node_modules/';
var primes = require(modules_path + 'primes/primes.js');

//var msg = primes.HelloWorld()


var is_prime = primes.sieve(20);
for (var i=2; i<=is_prime.length; i++) {
    if (is_prime[i] == 1) {
        console.log(i);
    }
}
